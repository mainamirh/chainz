export interface CoinRank {
  icon: string;
  Name: string;
  symbol: string;
  bestBuyPrice: number;
  buyExchange: string;
  bestSellPrice: number;
  sellExchange: string;
  marketCap: number;
  lastHour: number;
  lastDay: number;
  lastWeek: number;
  lastWeekChart: string;
}
